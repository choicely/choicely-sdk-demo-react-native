def ANDROID = gradle.includedBuild("android")

[
        "clean",
        "assembleDebug",
        "assembleOffline",
        "assembleRelease",
        "installDebug",
        "installOffline",
        "installRelease",
].each { t ->
    tasks.register(t) {
        group = "android"
        dependsOn ANDROID.task(":app:${t}")
    }
}
